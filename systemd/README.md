# Deprecated native units

These units are retained only as historical reference for installations made before the Docker migration. The Docker installer does not copy, enable, or invoke them. Docker Compose is the supported deployment method.
